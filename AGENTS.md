- Do not run, automate, or request permission to run tests against the real YouTube API or service, because they can trigger blocking and HTTP 403 responses; YouTube integration tests must be left for the user to run manually, though suggesting a specific manual test is allowed. Yoto integration tests may be proposed and run only after the user explicitly authorizes each run, because this application uses its registered Yoto app ID, but they must never be automated or repeated without fresh authorization.
